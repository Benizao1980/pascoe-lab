# Pascoe Lab website

Static GitHub Pages website for `pascoelab.com`.

## Publish

In the repository, open **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/ (root)`, then save.

The included `CNAME` file configures the custom domain. Do not change the DNS until the GitHub Pages preview works.
